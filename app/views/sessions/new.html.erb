<% provide(:title, "Sign in") %> <h1>Sign in</h1>

<div class="row">
	<div class="span6 offset3">
		<!--
			In users/new, there is a users model.  But there is no analog
			@session variable.  So instead of form_for(@user), which rails
			automatically knows will go to POST for /users, for sessions
			we need to indicate the *name* of the resource and the corresponding
			URL to POST to.
		-->
		<%= form_for(:session, url: sessions_path) do |f| %>
			<%= f.label :email %>
			<%= f.text_field :email %>

			<%= f.label :password %>
			<%= f.password_field :password %>

			<%= f.submit "Sign in", class: "btn btn-large btn-primary" %>
		<% end %>
		<p>New user? <%= link_to "Sign up now!", signup_path %></p>
	</div>
</div>
